[pytest]
# Unit tests live in tests/ and import the project packages from the root.
# scripts/ holds live checks (real browser, real Groq API) that are run by
# hand, never collected.
testpaths = tests
pythonpath = .
