# Screenshots

The agent saves one screenshot per step here, in a folder per run (`<run id>/001_iteration.png`, ...). Only the most recent runs are kept: 20 by default, set with `MAX_SCREENSHOT_RUNS` in `.env`. Everything except this file is git-ignored.
